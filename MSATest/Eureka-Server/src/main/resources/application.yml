server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false   # euraka의 registry에 등록할 지 여부 설정.
    fetch-registry: false         # registy에 있는 정보를 가져올 지 여부 결정.
