server:
  port: 8000

eureka:
  instance:
    prefer-ip-address: true         #Discovery? ??? ??? ?? ? IP Address? ??
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: gateway-service
  cloud:
    gateway:                        #gateway ?? ???? ??? ??
      default-filters:
        name: GlobalFilter          #?? ???? ?? ???? ?? ??
        args:                       #args? ?? ?? ?? ???? ??
          baseMessage: Hello Spring Cloud Gateway Global Filter
          preLogger: true
          postLogger: true
      routes:                       #??? ??
        id: member-service          #?????? ??
        uri: lb://MEMBER-SERVICE    #?????? ?? ??
        predicates:
          path: /actuator/info/member/**   #?? ???
        filters:                    #?? ??
          name: CustomFilter